package com.dolthhaven.dolt_mod_how.common.block;

import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;
import org.violetmoon.quark.addons.oddities.block.be.PipeBlockEntity;

public class ColoredEncasedPipeBlockEntity extends PipeBlockEntity {
    public ColoredEncasedPipeBlockEntity(BlockPos pos, BlockState state) {
        super(pos, state);
    }
}
